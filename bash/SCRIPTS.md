# 🛠️ Скрипты управления CoordViz

Набор удобных bash-скриптов для управления приложением CoordViz.

## 📜 Доступные скрипты

### 🚀 start.sh - Запуск приложения

Автоматически проверяет зависимости, собирает и запускает Docker контейнеры.

**Базовое использование:**
```bash
./start.sh
```

**С автоматическим открытием браузера:**
```bash
./start.sh --open
# или
./start.sh -o
```

**Что делает скрипт:**
- ✅ Проверяет наличие Docker и Docker Compose
- ✅ Проверяет, что Docker daemon запущен
- ✅ Собирает Docker образ
- ✅ Запускает контейнер
- ✅ Проверяет доступность приложения
- ✅ Показывает полезные команды

**Выход:**
- `0` - успешный запуск
- `1` - ошибка (отсутствуют зависимости или проблемы с запуском)

---

### 🛑 stop.sh - Остановка приложения

Останавливает и удаляет Docker контейнеры.

**Базовое использование:**
```bash
./stop.sh
```

**С очисткой volumes:**
```bash
./stop.sh --clean
# или
./stop.sh -c
```

**Полная очистка (включая образы):**
```bash
./stop.sh --full
# или
./stop.sh -f
```

**Опции:**
- Без флагов - останавливает контейнеры
- `--clean` / `-c` - останавливает и удаляет volumes
- `--full` / `-f` - останавливает и удаляет образы

---

### 📊 status.sh - Проверка статуса

Показывает текущий статус приложения и контейнера.

**Использование:**
```bash
./status.sh
```

**Что показывает:**
- ✅ Статус контейнера (запущен/остановлен/не найден)
- 📋 Информация о контейнере (имя, статус, порты)
- 📈 Использование ресурсов (CPU, память, сеть)
- 🌐 Доступность веб-приложения
- 💡 Подсказки по следующим действиям

---

## 🔄 Типичные сценарии использования

### Первый запуск
```bash
# 1. Запустить приложение
./start.sh --open

# 2. Проверить статус
./status.sh
```

### Разработка
```bash
# Запустить с hot reload
./start.sh

# Редактировать файлы в css/ и js/
# Изменения применяются автоматически, просто обновите страницу в браузере

# Просмотреть логи
docker-compose logs -f

# Перезапустить после изменений в конфигурации
docker-compose restart
```

### Остановка и очистка
```bash
# Остановить приложение
./stop.sh

# Остановить и очистить все данные
./stop.sh --full
```

### Проверка работоспособности
```bash
# Проверить статус
./status.sh

# Если приложение не отвечает
docker-compose logs

# Перезапустить
docker-compose restart
```

---

## 🔧 Ручное управление через Docker Compose

Если предпочитаете использовать Docker Compose напрямую:

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Логи
docker-compose logs -f

# Перезапуск
docker-compose restart

# Пересборка
docker-compose up -d --build

# Статус
docker-compose ps
```

---

## ❓ Решение проблем

### Проблема: Docker не запускается
```bash
# Проверьте, что Docker Desktop запущен
docker info

# Если не запущен, запустите Docker Desktop
```

### Проблема: Порт 8080 занят
```bash
# Измените порт в docker-compose.yml
# Замените "8080:80" на "8081:80" (или другой свободный порт)
```

### Проблема: Приложение не отвечает
```bash
# Посмотрите логи
docker-compose logs

# Проверьте статус
./status.sh

# Попробуйте перезапустить
docker-compose restart
```

### Проблема: Старая версия кэширована
```bash
# Пересоберите без кэша
docker-compose build --no-cache
docker-compose up -d
```

---

## 📝 Примечания

- Все скрипты требуют наличия Docker и Docker Compose
- Скрипты работают на Linux, macOS и Windows (Git Bash)
- Приложение доступно на `http://localhost:8080`
- Hot reload работает только для файлов HTML, CSS и JS
- Для изменений в Dockerfile требуется пересборка

---

## 🔗 Дополнительная информация

- Основная документация: [README.md](README.md)
- Конфигурация Docker: [docker-compose.yml](docker-compose.yml)
- Dockerfile: [Dockerfile](Dockerfile)

