# CoordinatesVisualization 🗺️

CoordViz — это веб-приложение для визуализации географических координат на интерактивной карте.
Пользователь может вставить координаты вручную, загрузить CSV-файл или фрагмент таблицы, а затем отфильтровать данные перед отображением.

## ✨ Основные возможности

### 📥 Импорт данных
- **Текстовый ввод**: Вставка координат в формате `lat,lon; lat,lon` или `lat,lon` (каждая пара на новой строке)
- **JSON формат**: Поддержка кастомных форматов, например `{"lat": 55.75, "lon": 37.62}`
- **CSV файлы**: Загрузка CSV-файлов с колонками `lat`, `lon`, `timestamp`, `name`
- **Вставка из буфера**: Ctrl+V для быстрой вставки CSV данных

### 🔍 Фильтрация данных (для CSV формата)
- **По месяцу (YYYY-MM)**: Отображаются только точки за выбранный период
- **По дате**: Фильтрация начиная с указанной даты
- **По георадиусу**: Отображаются точки в заданном радиусе (км) от выбранной точки на карте
- **Предпросмотр**: Возможность просмотра и подсчёта количества отфильтрованных строк

### 🌐 Дополнительные функции
- Интерактивная карта на базе Leaflet с использованием OpenStreetMap
- Автоматическое масштабирование карты под загруженные точки
- Ссылки на панорамы Яндекс.Карт для каждой точки
- Красивый современный UI с анимациями

## 🚀 Быстрый старт

### Вариант 1: Использование bash скрипта (самый простой)

1. **Клонируйте репозиторий**:
```bash
git clone <repository-url>
cd coordinates_visualization
```

2. **Запустите приложение одной командой**:
```bash
./start.sh
```

Скрипт автоматически:
- Проверит наличие Docker и Docker Compose
- Соберет и запустит контейнеры
- Проверит доступность приложения
- Покажет полезные команды

3. **Остановка приложения**:
```bash
./stop.sh
```

**Проверка статуса**:
```bash
./status.sh
```

**Дополнительные опции**:
- `./start.sh --open` — запустить и открыть браузер автоматически
- `./stop.sh --clean` — остановить и очистить volumes
- `./stop.sh --full` — остановить и удалить образы
- `./status.sh` — проверить статус приложения и использование ресурсов

📚 Подробная документация по скриптам: [SCRIPTS.md](SCRIPTS.md)

### Вариант 2: Использование Docker Compose напрямую

1. **Клонируйте репозиторий**:
```bash
git clone <repository-url>
cd coordinates_visualization
```

2. **Запустите приложение**:
```bash
docker-compose up -d
```

3. **Откройте в браузере**:
```
http://localhost:8080
```

4. **Остановка приложения**:
```bash
docker-compose down
```

### Разработка с hot reload

Docker Compose настроен с volume mapping для автоматической перезагрузки изменений:
```bash
docker-compose up
```

Теперь любые изменения в файлах `index.html`, `css/`, `js/` будут сразу видны в браузере после обновления страницы.

### Без Docker

Просто откройте `index.html` в современном браузере. Все зависимости (Leaflet) загружаются через CDN.

## 📦 Production build

Для production используется оптимизированный multi-stage Docker образ:

```bash
docker build --target production -t coordviz:latest .
docker run -p 80:80 coordviz:latest
```

## 🛠️ Технологии

- **Frontend**: Vanilla JavaScript, HTML5, CSS3
- **Карты**: Leaflet.js + OpenStreetMap
- **Web Server**: Nginx (Alpine Linux)
- **Контейнеризация**: Docker, Docker Compose

## 📝 Форматы данных

### Текстовый ввод
```
55.7558,37.6173
59.9343,30.3351
```

### JSON
```json
{"lat": 55.7558, "lon": 37.6173, "name": "Москва"}
```

или массив:
```json
[
  {"lat": 55.7558, "lon": 37.6173, "timestamp": "2024-01-15"},
  {"lat": 59.9343, "lon": 30.3351, "timestamp": "2024-01-16"}
]
```

### CSV
```csv
lat,lon,timestamp,name
55.7558,37.6173,2024-01-15,Москва
59.9343,30.3351,2024-01-16,Санкт-Петербург
```

## 📄 Лицензия

MIT